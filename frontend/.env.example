# Express API origin (http or https only; no path — e.g. https://api.example.com)
BACKEND_URL=http://127.0.0.1:4000

# Must match backend JWT_SECRET (middleware verifies the httpOnly cookie)
JWT_SECRET=change-this-to-a-long-random-string-at-least-16-chars
