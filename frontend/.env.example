# Proxy API during `next dev` / production (Express URL)
BACKEND_URL=http://127.0.0.1:4000

# Must match backend JWT_SECRET (used by middleware to verify the cookie)
JWT_SECRET=change-this-to-a-long-random-string-at-least-16-chars
